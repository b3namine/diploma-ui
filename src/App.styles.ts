import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    container: {
        marginTop: 60
    },
    footer: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        padding: '48px 12px'
    }
})
